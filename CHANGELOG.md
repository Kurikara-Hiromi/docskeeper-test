# Changelog

## [Unreleased]

### Added

- `formatBytes` helper function exports human-readable byte sizes (B, KB, MB, GB).
- `formatDuration` helper function exports human-readable time durations (ms, s, m).
