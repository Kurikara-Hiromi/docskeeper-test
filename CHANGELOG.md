# Changelog

## [Unreleased]

### Added

- `throttle` function to limit repeated function calls to at most once per specified interval
- `ThrottleOptions` type with `intervalMs` and optional `leading` configuration
